<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Theme applied to the Android Window while the process is starting when the OS's Dark Mode setting is off -->
    <!-- Simplified to match iOS: black background with centered logo -->
    <style name="LaunchTheme" parent="@android:style/Theme.Black.NoTitleBar">
        <item name="android:forceDarkAllowed">false</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
        <item name="android:windowLayoutInDisplayCutoutMode">shortEdges</item>
        <!-- Black background for splash screen -->
        <item name="android:windowSplashScreenBackground">@color/launch_background_color</item>
        <!-- Fallback windowBackground for compatibility (contains logo for older devices/first install) -->
        <!-- CRITICAL: windowBackground must be set for first install compatibility -->
        <item name="android:windowBackground">@drawable/launch_background</item>
        <!-- Use properly sized XML drawable (50dp) for Android 12+ splash icon - logo sits directly on black bg -->
        <item name="android:windowSplashScreenAnimatedIcon">@drawable/splash_icon_small</item>
        <!-- Transparent icon background to remove white circle -->
        <item name="android:windowSplashScreenIconBackgroundColor">@android:color/transparent</item>
    </style>
    <!-- Theme applied to the Android Window as soon as the process has started.
         This theme determines the color of the Android Window while your
         Flutter UI initializes, as well as behind your Flutter UI while its
         running.
         
         This Theme is only used starting with V2 of Flutter's Android embedding. -->
    <style name="NormalTheme" parent="@android:style/Theme.Light.NoTitleBar">
        <item name="android:windowBackground">@drawable/launch_background</item>
    </style>
</resources>
