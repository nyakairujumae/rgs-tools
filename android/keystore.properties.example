# Android Release Keystore Configuration
# Copy this file to keystore.properties and fill in your actual values
# DO NOT commit keystore.properties to version control

storePassword=your_keystore_password
keyPassword=your_key_password
keyAlias=your_key_alias
storeFile=../keystore/rgs-release-key.jks

# Instructions:
# 1. Generate a keystore:
#    keytool -genkey -v -keystore rgs-release-key.jks -keyalg RSA -keysize 2048 -validity 10000 -alias rgs-key
# 2. Copy this file to keystore.properties
# 3. Fill in the actual passwords and alias
# 4. Place the .jks file in android/keystore/ directory
# 5. Add keystore.properties to .gitignore



